# Runtime dependency for rank.py (the 5-minute reproduce command).
numpy==2.2.6

# Only needed for precompute_embeddings.py (offline, not part of the
# 5-minute ranking budget) and for re-running the sandbox demo app.
scikit-learn==1.7.2
scipy==1.15.3
joblib==1.5.3

# Sandbox demo app (app.py)
gradio==4.44.1
